<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>{POST}er Code Learners’ Blog</title>
  
  <link rel="stylesheet" href="/stylesheets/style.css">
</head>

<body>
  <div class="wrapper">
    <header>
      <h1>
        <a href="/">{POST}er</a>
      </h1>
      <nav>
        <a href="/">Home</a> | 
        {{#if loggedIn}}
        <a href="/dashboard">Dashboard</a> |
        <button id="logout" class="btn-no-style">Logout</button>
        {{else}}
        <a href="/login">Dashboard</a>
        |
        <a href="/login">Login</a>
        {{/if}}
      </nav>
    </header>
    <main>
      {{{ body }}}
    </main>
    <footer>
      <span class="POST">{POST}er</span>&nbsp;is a tech blog for people to share their experiences of learning to code.
    </footer>
  </div>
  {{#if loggedIn}}
  <script src="/javascript/logout.js"></script>
  {{/if}}
</body>

</html>